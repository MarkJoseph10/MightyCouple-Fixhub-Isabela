import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mightycouple.commerce",
  appName: "Mighty Couple",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
